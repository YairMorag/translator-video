# Supabase (not required to run the POC — the app runs on in-memory mock
# data by default). Fill these in when wiring up real persistence + auth.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
